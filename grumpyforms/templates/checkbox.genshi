<input xmlns:py="http://genshi.edgewall.org/"
	type="checkbox"
	id="${id}" name="${name}"
	value="${option_value}"
	disabled="${'disabled' if (disabled == True) else None}"
	checked="${'checked' if (value == True) else None}" class="${css_classes}" />
